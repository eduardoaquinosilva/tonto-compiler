// constantes para os tokens 
enum {
    // Palavras reservadas
    GENSET=1, DISJOINT, COMPLETE, GENERAL, SPECIFICS, WHERE, PACKAGE, IMPORT, FUNCTIONAL_COMPLEXES,

    // Esteriótipos de classe
    CLASS_STEREOTYPE,

    // Esteriótipos de relações
    RELATIONS_STEREOTYPE,

    // Meta-atributos
    META,

    // Tipos de dados nativos
    TYPE,

    // Novos tipos 
    NEW_TYPE,

    // Símbolos especiais
    LBRACE, RBRACE, LBRACKET, RBRACKET, LP, RP, COLON, COMMA, DOTDOT, LRELATION, RRELATION, ASTHERISTICS, AT,

    // Declaração de instancia
    INSTANCE_NAME,

    // Declaração de classe
    CLASS_NAME,

    // Declaração de relação
    RELATION_NAME,

    // Números
    NUMBER
};
